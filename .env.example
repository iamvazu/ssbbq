# Copy this file to .env.local for local development with `vercel dev`.
# In production, add these as Environment Variables in the Vercel dashboard
# (Project → Settings → Environment Variables) — or let the Upstash
# integration fill them in for you automatically (see README.md).

UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
